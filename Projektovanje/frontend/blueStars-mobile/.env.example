# Copy to .env.local (ignored by Git).
# For a physical phone, replace localhost with the development computer's LAN IP.
EXPO_PUBLIC_API_SCHEME=http
EXPO_PUBLIC_API_ADDRESS=localhost
EXPO_PUBLIC_API_PORT=8080
