import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    width: "100%",
    position: "relative",
  },
  input: {
    minHeight: 110,
    paddingRight: 44,
  },
  pencil: {
    position: "absolute",
    right: 12,
    bottom: 12,
  },
});

export default styles;
